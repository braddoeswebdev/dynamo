<h1>Listing pages</h1>

<table class="table table-striped table-bordered">
<% @pages.in_groups_of(5) do |group| %>
  <tr>
	  <% group.each do |page| %>
		<td width="20%"><%= page ? link_to(page.title, page) : "" %></td>
	  <% end %>
  </tr>
<% end %>
</table>
